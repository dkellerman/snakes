Snake Game, by David Kellerman

To run: just open index.html in a browser, and use arrow keys to control the snake.

Next steps:
* Too many things are hardcoded or not configurable, e.g. element IDs
* Implement post-1950's look and feel